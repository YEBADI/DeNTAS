#To address issue #17; I investigated generating local db at user request but this proved to be too difficult
#instead I'll prepare a list of species and pre-generate a local db for each one
#then user will be prompted with a drop down list and can select which species they want to BLAST their input uploads against

#model species P. alecto, then common major species mouse and human.

import os
# make local db for P. alecto
cmd="makeblastdb -in P_Alecto.fasta.txt -parse_seqids -dbtype nucl"

os.system(cmd)

#make local db for H. sapiens
cmd="makeblastdb -in H_Sapiens.fasta.txt -parse_seqids -dbtype nucl"

os.system(cmd)

#make local db for M. musculus
cmd="makeblastdb -in M_Musculus.fasta.txt -parse_seqids -dbtype nucl"

os.system(cmd)

